const dbquery = require('../dbConnection');


async function setDeviceStatus(id, status) {
  let deviceStatus = await dbquery('UPDATE `network_devices` SET `status`="' + status + '" WHERE `id`=' + id);
  return deviceStatus;
}

module.exports = setDeviceStatus;
